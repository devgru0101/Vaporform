#Project Vision

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vaporform - Step 1: Project Vision</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;700&family=Inter:wght@400;500;700;900&family=JetBrains+Mono:wght@400;700&display=swap');
        
        :root {
            /* Base Colors */
            --vf-bg-primary: #000000;
            --vf-bg-secondary: #0A0A0A;
            --vf-bg-tertiary: #111111;
            --vf-bg-elevated: #1A1A1A;
            --vf-surface-border: #2A2A2A;
            --vf-surface-border-light: #3A3A3A;
            
            /* Text Colors */
            --vf-text-primary: #F6EEE3;
            --vf-text-secondary: #CAC4B7;
            --vf-text-muted: #737664;
            --vf-text-disabled: #4A4A4A;
            
            /* Accent Colors */
            --vf-accent-primary: #CAC4B7;
            --vf-accent-active: #F6EEE3;
            --vf-accent-success: #E1FF00;
            --vf-accent-danger: #FF4200;
            --vf-accent-info: #2B00FF;
            
            /* Typography */
            --vf-font-display: 'Space Grotesk', 'Inter', sans-serif;
            --vf-font-body: 'Inter', system-ui, sans-serif;
            --vf-font-mono: 'JetBrains Mono', monospace;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: var(--vf-font-body);
            background: var(--vf-bg-primary);
            color: var(--vf-text-primary);
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        /* Modal Overlay */
        .modal-overlay {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.9);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 1000;
        }
        
        /* Modal Container */
        .modal-container {
            background: var(--vf-bg-secondary);
            border: 1px solid var(--vf-surface-border);
            width: 90%;
            max-width: 1000px;
            max-height: 90vh;
            display: flex;
            flex-direction: column;
        }
        
        /* Modal Header */
        .modal-header {
            padding: 20px 24px;
            border-bottom: 1px solid var(--vf-surface-border);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .modal-title {
            font-family: var(--vf-font-display);
            font-size: 18px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }
        
        .close-btn {
            width: 32px;
            height: 32px;
            display: flex;
            align-items: center;
            justify-content: center;
            background: transparent;
            border: 1px solid var(--vf-surface-border);
            color: var(--vf-text-muted);
            cursor: pointer;
            transition: all 150ms ease-out;
        }
        
        .close-btn:hover {
            border-color: var(--vf-accent-danger);
            color: var(--vf-accent-danger);
        }
        
        /* Progress Bar */
        .progress-bar {
            height: 2px;
            background: var(--vf-surface-border);
            position: relative;
        }
        
        .progress-fill {
            height: 100%;
            background: var(--vf-accent-primary);
            width: 20%;
            transition: width 300ms ease-out;
        }
        
        /* Modal Body */
        .modal-body {
            flex: 1;
            display: flex;
            overflow: hidden;
        }
        
        /* Wizard Steps Sidebar */
        .wizard-sidebar {
            width: 240px;
            background: var(--vf-bg-tertiary);
            border-right: 1px solid var(--vf-surface-border);
            padding: 24px 0;
        }
        
        .step-item {
            padding: 12px 24px;
            display: flex;
            align-items: center;
            gap: 12px;
            color: var(--vf-text-muted);
            font-size: 11px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            cursor: pointer;
            transition: all 150ms ease-out;
            position: relative;
        }
        
        .step-item.active {
            color: var(--vf-accent-active);
            background: var(--vf-bg-elevated);
        }
        
        .step-item.active::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            bottom: 0;
            width: 2px;
            background: var(--vf-accent-primary);
        }
        
        .step-number {
            width: 24px;
            height: 24px;
            border: 1px solid var(--vf-surface-border);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 10px;
            font-weight: 700;
        }
        
        .step-item.active .step-number {
            background: var(--vf-accent-primary);
            color: var(--vf-bg-primary);
            border-color: var(--vf-accent-primary);
        }
        
        /* Wizard Content Area */
        .wizard-content {
            flex: 1;
            padding: 32px;
            overflow-y: auto;
        }
        
        .step-title {
            font-family: var(--vf-font-display);
            font-size: 24px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: -0.02em;
            margin-bottom: 8px;
        }
        
        .step-description {
            color: var(--vf-text-muted);
            font-size: 14px;
            margin-bottom: 32px;
            line-height: 1.5;
        }
        
        /* Form Elements */
        .form-group {
            margin-bottom: 24px;
        }
        
        .form-label {
            display: block;
            font-size: 11px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            color: var(--vf-text-secondary);
            margin-bottom: 8px;
        }
        
        .form-input {
            width: 100%;
            padding: 10px 12px;
            background: var(--vf-bg-primary);
            border: 1px solid var(--vf-surface-border);
            color: var(--vf-text-primary);
            font-family: var(--vf-font-mono);
            font-size: 13px;
            transition: border-color 150ms ease-out;
        }
        
        .form-input:focus {
            outline: none;
            border-color: var(--vf-accent-primary);
        }
        
        .form-textarea {
            width: 100%;
            padding: 12px;
            background: var(--vf-bg-primary);
            border: 1px solid var(--vf-surface-border);
            color: var(--vf-text-primary);
            font-family: var(--vf-font-body);
            font-size: 13px;
            line-height: 1.5;
            min-height: 120px;
            resize: vertical;
            transition: border-color 150ms ease-out;
        }
        
        .form-textarea:focus {
            outline: none;
            border-color: var(--vf-accent-primary);
        }
        
        .form-hint {
            font-size: 11px;
            color: var(--vf-text-muted);
            margin-top: 4px;
        }
        
        .char-count {
            text-align: right;
            font-size: 10px;
            color: var(--vf-text-muted);
            margin-top: 4px;
        }
        
        /* AI Suggestions Panel */
        .ai-suggestions-panel {
            background: var(--vf-bg-tertiary);
            border: 1px solid var(--vf-surface-border);
            padding: 16px;
            margin: 24px 0;
        }
        
        .ai-panel-header {
            display: flex;
            align-items: center;
            gap: 8px;
            margin-bottom: 12px;
        }
        
        .claude-icon {
            width: 20px;
            height: 20px;
            background: var(--vf-accent-primary);
            border-radius: 2px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 12px;
            font-weight: bold;
            color: var(--vf-bg-primary);
        }
        
        .ai-panel-title {
            font-size: 12px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }
        
        .ai-analyzing {
            display: flex;
            align-items: center;
            gap: 8px;
            color: var(--vf-text-muted);
            font-size: 12px;
        }
        
        .analyzing-dot {
            width: 4px;
            height: 4px;
            background: var(--vf-accent-primary);
            border-radius: 50%;
            animation: pulse 1.5s infinite;
        }
        
        .analyzing-dot:nth-child(2) { animation-delay: 0.2s; }
        .analyzing-dot:nth-child(3) { animation-delay: 0.4s; }
        
        @keyframes pulse {
            0%, 60%, 100% { opacity: 0.3; }
            30% { opacity: 1; }
        }
        
        .suggestion-cards {
            display: flex;
            flex-direction: column;
            gap: 12px;
        }
        
        .suggestion-card {
            background: var(--vf-bg-secondary);
            border: 1px solid var(--vf-surface-border);
            padding: 12px;
        }
        
        .suggestion-title {
            font-size: 11px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            color: var(--vf-text-secondary);
            margin-bottom: 8px;
        }
        
        .suggestion-content {
            font-size: 12px;
            color: var(--vf-text-secondary);
            line-height: 1.4;
        }
        
        .suggestion-items {
            display: flex;
            flex-wrap: wrap;
            gap: 6px;
            margin-top: 8px;
        }
        
        .suggestion-tag {
            padding: 4px 8px;
            background: var(--vf-bg-tertiary);
            border: 1px solid var(--vf-surface-border);
            font-size: 11px;
            color: var(--vf-text-secondary);
        }
        
        /* Tag Input */
        .tag-input-container {
            background: var(--vf-bg-primary);
            border: 1px solid var(--vf-surface-border);
            padding: 8px;
            min-height: 44px;
            display: flex;
            flex-wrap: wrap;
            gap: 6px;
            align-items: center;
            transition: border-color 150ms ease-out;
        }
        
        .tag-input-container:focus-within {
            border-color: var(--vf-accent-primary);
        }
        
        .tag {
            padding: 4px 8px;
            background: var(--vf-bg-tertiary);
            border: 1px solid var(--vf-surface-border);
            font-size: 11px;
            display: flex;
            align-items: center;
            gap: 6px;
        }
        
        .tag-remove {
            cursor: pointer;
            color: var(--vf-text-muted);
            transition: color 150ms ease-out;
        }
        
        .tag-remove:hover {
            color: var(--vf-accent-danger);
        }
        
        .tag-input {
            flex: 1;
            min-width: 150px;
            border: none;
            background: transparent;
            color: var(--vf-text-primary);
            font-family: var(--vf-font-mono);
            font-size: 12px;
            outline: none;
        }
        
        /* Project Goals */
        .goals-list {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }
        
        .goal-item {
            display: flex;
            gap: 8px;
            align-items: center;
        }
        
        .goal-input {
            flex: 1;
            padding: 8px 12px;
            background: var(--vf-bg-primary);
            border: 1px solid var(--vf-surface-border);
            color: var(--vf-text-primary);
            font-size: 12px;
        }
        
        .goal-remove {
            width: 28px;
            height: 28px;
            display: flex;
            align-items: center;
            justify-content: center;
            background: transparent;
            border: 1px solid var(--vf-surface-border);
            color: var(--vf-text-muted);
            cursor: pointer;
            transition: all 150ms ease-out;
        }
        
        .goal-remove:hover {
            border-color: var(--vf-accent-danger);
            color: var(--vf-accent-danger);
        }
        
        .add-goal-btn {
            padding: 8px 16px;
            background: transparent;
            border: 1px solid var(--vf-surface-border);
            color: var(--vf-text-secondary);
            font-size: 11px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            cursor: pointer;
            transition: all 150ms ease-out;
            margin-top: 8px;
        }
        
        .add-goal-btn:hover {
            border-color: var(--vf-text-secondary);
            background: var(--vf-bg-elevated);
        }
        
        /* Modal Footer */
        .modal-footer {
            padding: 20px 24px;
            border-top: 1px solid var(--vf-surface-border);
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: var(--vf-bg-tertiary);
        }
        
        .footer-info {
            font-size: 11px;
            color: var(--vf-text-muted);
        }
        
        .footer-actions {
            display: flex;
            gap: 12px;
        }
        
        .btn {
            padding: 8px 20px;
            font-size: 11px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            border: none;
            cursor: pointer;
            transition: all 150ms ease-out;
            background: transparent;
        }
        
        .btn-secondary {
            border: 1px solid var(--vf-surface-border);
            color: var(--vf-text-secondary);
        }
        
        .btn-secondary:hover {
            border-color: var(--vf-text-secondary);
            background: var(--vf-bg-elevated);
        }
        
        .btn-primary {
            background: var(--vf-text-muted);
            color: var(--vf-bg-primary);
        }
        
        .btn-primary:hover {
            background: var(--vf-accent-primary);
        }
        
        /* Icon styles */
        .icon {
            width: 14px;
            height: 14px;
            stroke: currentColor;
            stroke-width: 2;
            fill: none;
            display: inline-block;
            vertical-align: middle;
        }
    </style>
</head>
<body>
    <!-- Modal Overlay -->
    <div class="modal-overlay">
        <div class="modal-container">
            <!-- Modal Header -->
            <div class="modal-header">
                <h2 class="modal-title">Create New Project</h2>
                <button class="close-btn">×</button>
            </div>
            
            <!-- Progress Bar -->
            <div class="progress-bar">
                <div class="progress-fill"></div>
            </div>
            
            <!-- Modal Body -->
            <div class="modal-body">
                <!-- Wizard Steps Sidebar -->
                <div class="wizard-sidebar">
                    <div class="step-item active">
                        <div class="step-number">1</div>
                        <span>Project Vision</span>
                    </div>
                    <div class="step-item">
                        <div class="step-number">2</div>
                        <span>Tech Stack</span>
                    </div>
                    <div class="step-item">
                        <div class="step-number">3</div>
                        <span>Integrations</span>
                    </div>
                    <div class="step-item">
                        <div class="step-number">4</div>
                        <span>Configuration</span>
                    </div>
                    <div class="step-item">
                        <div class="step-number">5</div>
                        <span>Review & Generate</span>
                    </div>
                </div>
                
                <!-- Wizard Content Area -->
                <div class="wizard-content">
                    <h3 class="step-title">Describe Your Project Vision</h3>
                    <p class="step-description">Tell Claude what you want to build - be as detailed as you like. The more context you provide, the better Claude can tailor your project.</p>
                    
                    <!-- Project Name -->
                    <div class="form-group">
                        <label class="form-label">Project Name</label>
                        <input type="text" class="form-input" placeholder="e.g., TaskFlow Pro, EcoMarket, GameHub" />
                        <div class="form-hint">Choose a memorable name for your project</div>
                    </div>
                    
                    <!-- Project Description -->
                    <div class="form-group">
                        <label class="form-label">Project Description</label>
                        <textarea class="form-textarea" placeholder="Describe your project in detail. What problem does it solve? Who is it for? What makes it unique? Example: 'I want to build a task management app that helps remote teams collaborate more effectively. It should have real-time updates, kanban boards, and integrate with popular tools like Slack and GitHub...'"></textarea>
                        <div class="char-count">0 / 1000 characters</div>
                    </div>
                    
                    <!-- AI Suggestions Panel (shows after typing) -->
                    <div class="ai-suggestions-panel">
                        <div class="ai-panel-header">
                            <div class="claude-icon">C</div>
                            <span class="ai-panel-title">Claude's Analysis</span>
                        </div>
                        <div class="ai-analyzing">
                            <span>Analyzing your project description</span>
                            <span class="analyzing-dot"></span>
                            <span class="analyzing-dot"></span>
                            <span class="analyzing-dot"></span>
                        </div>
                        <div class="suggestion-cards" style="display: none;">
                            <div class="suggestion-card">
                                <div class="suggestion-title">Recommended Architecture</div>
                                <div class="suggestion-content">
                                    Based on your description, a microservices architecture with Encore.ts would work well for scalable task management.
                                </div>
                            </div>
                            <div class="suggestion-card">
                                <div class="suggestion-title">Key Features to Consider</div>
                                <div class="suggestion-items">
                                    <span class="suggestion-tag">Real-time sync</span>
                                    <span class="suggestion-tag">User authentication</span>
                                    <span class="suggestion-tag">File uploads</span>
                                    <span class="suggestion-tag">Notifications</span>
                                    <span class="suggestion-tag">Analytics</span>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Core Functionality -->
                    <div class="form-group">
                        <label class="form-label">Core Functionality</label>
                        <textarea class="form-textarea" placeholder="What specific features and capabilities should your app have? List the main user flows and interactions. Example: 'Users should be able to create projects, invite team members, create and assign tasks, set due dates, add comments, upload files...'"></textarea>
                        <div class="char-count">0 / 2000 characters</div>
                    </div>
                    
                    <!-- Target Audience -->
                    <div class="form-group">
                        <label class="form-label">Target Audience</label>
                        <input type="text" class="form-input" placeholder="e.g., Small business owners, Students, Developers, General consumers" />
                        <div class="form-hint">Who will be using your application?</div>
                    </div>
                    
                    <!-- Inspiration Apps -->
                    <div class="form-group">
                        <label class="form-label">Inspiration Apps (Optional)</label>
                        <div class="tag-input-container">
                            <div class="tag">
                                Notion
                                <span class="tag-remove">×</span>
                            </div>
                            <div class="tag">
                                Trello
                                <span class="tag-remove">×</span>
                            </div>
                            <input type="text" class="tag-input" placeholder="Add app names..." />
                        </div>
                        <div class="form-hint">Apps or websites that inspire your vision</div>
                    </div>
                    
                    <!-- Project Goals -->
                    <div class="form-group">
                        <label class="form-label">Project Goals</label>
                        <div class="goals-list">
                            <div class="goal-item">
                                <input type="text" class="goal-input" placeholder="e.g., Launch MVP within 3 months" value="Build a scalable SaaS platform" />
                                <button class="goal-remove">×</button>
                            </div>
                            <div class="goal-item">
                                <input type="text" class="goal-input" placeholder="e.g., Support 10,000 concurrent users" />
                                <button class="goal-remove">×</button>
                            </div>
                        </div>
                        <button class="add-goal-btn">+ Add Goal</button>
                    </div>
                </div>
            </div>
            
            <!-- Modal Footer -->
            <div class="modal-footer">
                <div class="footer-info">
                    Step 1 of 5 • Project Vision
                </div>
                <div class="footer-actions">
                    <button class="btn btn-secondary">Cancel</button>
                    <button class="btn btn-primary">Next: Choose Tech Stack</button>
                </div>
            </div>
        </div>
    </div>
</body>
</html>